"""Automated literature discovery agent (PR8b).

Distinct from the existing ``LiteratureAgent`` (which retrieves already-ingested
KnowledgeStore entities): this agent discovers *external* papers and returns a typed
evidence bundle of metadata and relevance — never a biological claim, and never a
write to the KnowledgeStore.
"""
