# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
build/
dist/
.installed.cfg

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Environment / secrets
.env
*.local

# Data / models (do not commit large artifacts)
data/
*.pt
*.ckpt
*.parquet

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json

# Databases (local volumes)
docker/volumes/
neo4j/
qdrant_storage/

# Unattended-run scratch artifacts (docs/operations/routine_runbook.md)
# Root-anchored on purpose: these are the gate's request files and its lock/confirmation/
# proceed/completion token directory, written into the repository root by a scheduled run.
# They are transient outputs, never repository inputs — the durable run state lives in
# refs/heads/vcrp-automation/*, and the run record lives on issue #21.
/phase1.json
/phase2.json
/.automation/
